var React = require('react');
var ReactDOM = require('react-dom');
var WeatherManager = require('./components/Weather/WeatherManager.jsx');

ReactDOM.render(<WeatherManager />, document.getElementById('weather'));
